---
# argument spec file for sflab.homelab.netbox_init

argument_specs:
  main:
    short_description: Initialize NetBox with default data.
    # options:
    #   docker_reverse_proxy_type:
    #     type: str
    #     description: Type of reverse proxy to deploy (traefik or caddy).
    #     default: "caddy"
    #     choices:
    #       - traefik
    #       - caddy
