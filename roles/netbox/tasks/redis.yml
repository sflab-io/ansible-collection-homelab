---
- name: Install redis
  ansible.builtin.package:
    name: redis
    state: present

- name: Ensure redis is running and enabled
  ansible.builtin.service:
    name: redis
    state: started
    enabled: true
